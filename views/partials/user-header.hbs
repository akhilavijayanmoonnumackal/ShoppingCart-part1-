<nav class="navbar navbar-expand-lg bg-secondary bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold fs-4" style="color: bisque;" href="#">PERFUME</a>

      <div class="dropdown">
        <button class="btn btn-primary dropdown-toggle bg-success" type="button" id="dropdownMenu2" data-bs-toggle="dropdown" aria-expanded="false">
          {{#if user}}
          {{user.name}}
          {{else}}
          Account
          {{/if}}
        </button>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenu2" >
          {{#if user}}
          <a href="/logout" style="text-decoration: none;"><li><button class="dropdown-item" type="button">Logout</button></li></a>
          {{else}}
          <a href="/login" style="text-decoration: none;"><li><button class="dropdown-item" type="button">Login</button></li></a>
          {{/if}}
        </ul>
      </div>

  </div>
</nav>